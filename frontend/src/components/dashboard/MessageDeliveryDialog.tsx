import { useEffect, useMemo, useRef, useState, type RefObject } from "react";
import { motion, useReducedMotion } from "motion/react";
import { createPortal } from "react-dom";
import { supabase } from "../../lib/supabase";
import type { AcceptedConversationItem } from "../../types/conversations";
import ProfileAvatar from "./ProfileAvatar";
import { getConversationDisplayName } from "./profileUtils";
import { formatScheduledInstant, formatScheduledInstantAccessible, getDefaultScheduledLocalTime, parseLocalScheduledTime } from "./scheduledMessageTime";

export type MessageDeliveryDraft =
  | { kind: "forward"; sourceMessageId: string; preview: string }
  | { kind: "note"; body: string; preview: string; omittedAttachmentCount: number; wasTruncated: boolean };

type MessageDeliveryDialogProps = { conversations: AcceptedConversationItem[]; currentUserId: string; draft: MessageDeliveryDraft; returnFocusRef: RefObject<HTMLElement | null>; onClose: () => void; onOpenConversation: (conversation: AcceptedConversationItem) => void; onSent: () => void };
type DeliverySuccess = { kind: "sent"; conversationId: string } | { kind: "scheduled"; conversationId: string; scheduledFor: string };

function normalizeDeliveryError(error: { code?: string } | null, scheduling = false) {
  if (!navigator.onLine) return "You're offline. Reconnect and try again.";
  if (error?.code === "42501" || error?.code === "PGRST301") return "That conversation is no longer available for sending.";
  if (error?.code === "P0002") return "The original message is no longer available to forward.";
  if (error?.code === "22023" || error?.code === "23514") return scheduling ? "Check the message, date, and time and try again." : "This content can't be sent as a message.";
  return scheduling ? "Nemissive couldn't schedule this message. Please try again." : "Nemissive couldn't send this message. Please try again.";
}

function SendIcon() { return <svg viewBox="0 0 24 24" fill="none" stroke="currentColor" strokeWidth="1.8" className="h-5 w-5" aria-hidden="true"><path d="m4 5 16 7-16 7 3-7-3-7Zm3 7h13" strokeLinecap="round" strokeLinejoin="round" /></svg>; }

function MessageDeliveryDialog({ conversations, currentUserId, draft, returnFocusRef, onClose, onOpenConversation, onSent }: MessageDeliveryDialogProps) {
  const reducedMotion = useReducedMotion();
  const panelRef = useRef<HTMLDivElement>(null);
  const onCloseRef = useRef(onClose);
  const isSubmittingRef = useRef(false);
  const [query, setQuery] = useState("");
  const [selectedConversationId, setSelectedConversationId] = useState<string | null>(null);
  const [mode, setMode] = useState<"choose" | "schedule">("choose");
  const [dateValue, setDateValue] = useState("");
  const [timeValue, setTimeValue] = useState("");
  const [isSubmitting, setIsSubmitting] = useState(false);
  const [error, setError] = useState("");
  const [success, setSuccess] = useState<DeliverySuccess | null>(null);

  useEffect(() => { onCloseRef.current = onClose; }, [onClose]);
  const filteredConversations = useMemo(() => { const normalized = query.trim().toLocaleLowerCase(); return normalized ? conversations.filter((conversation) => getConversationDisplayName(conversation.otherProfile, conversation.otherNickname).toLocaleLowerCase().includes(normalized) || (conversation.otherProfile.username?.toLocaleLowerCase() ?? "").includes(normalized)) : conversations; }, [conversations, query]);
  const selectedConversation = conversations.find((conversation) => conversation.conversationId === selectedConversationId) ?? null;
  const successConversation = conversations.find((conversation) => conversation.conversationId === success?.conversationId) ?? null;
  const parsedSchedule = mode === "schedule" ? { ...parseLocalScheduledTime(dateValue, timeValue), error: "" } : { instant: null, error: "" };

  useEffect(() => {
    const previousOverflow = document.body.style.overflow; const returnTo = returnFocusRef.current; document.body.style.overflow = "hidden";
    function handleKeyDown(event: KeyboardEvent) {
      if (event.key === "Escape" && !isSubmittingRef.current) { event.preventDefault(); onCloseRef.current(); return; }
      if (event.key !== "Tab" || !panelRef.current) return;
      const controls = [...panelRef.current.querySelectorAll<HTMLElement>('button:not([disabled]),input:not([disabled]),[tabindex]:not([tabindex="-1"])')]; const first = controls[0]; const last = controls.at(-1); if (!first || !last) return;
      if (event.shiftKey && document.activeElement === first) { event.preventDefault(); last.focus(); } else if (!event.shiftKey && document.activeElement === last) { event.preventDefault(); first.focus(); }
    }
    const frame = window.requestAnimationFrame(() => panelRef.current?.querySelector<HTMLElement>("[data-autofocus],button:not([disabled])")?.focus()); document.addEventListener("keydown", handleKeyDown);
    return () => { window.cancelAnimationFrame(frame); document.body.style.overflow = previousOverflow; document.removeEventListener("keydown", handleKeyDown); window.requestAnimationFrame(() => returnTo?.isConnected && returnTo.focus()); };
  }, [returnFocusRef]);
  useEffect(() => { if (!success) return; const frame = window.requestAnimationFrame(() => panelRef.current?.querySelector<HTMLElement>("[data-success-autofocus]")?.focus()); return () => window.cancelAnimationFrame(frame); }, [success]);

  async function sendNow() {
    if (!selectedConversation || isSubmitting) return;
    isSubmittingRef.current = true; setIsSubmitting(true); setError("");
    const result = draft.kind === "forward" ? await supabase.rpc("forward_text_message", { source_message_id: draft.sourceMessageId, target_conversation_id: selectedConversation.conversationId }) : await supabase.from("messages").insert({ conversation_id: selectedConversation.conversationId, sender_id: currentUserId, body: draft.body }).select("id").single();
    isSubmittingRef.current = false; setIsSubmitting(false);
    if (result.error || !result.data) { setError(normalizeDeliveryError(result.error)); return; }
    setSuccess({ kind: "sent", conversationId: selectedConversation.conversationId }); onSent();
  }

  function beginScheduling() { if (draft.kind !== "note" || !selectedConversation) return; const initial = getDefaultScheduledLocalTime(); setDateValue(initial.date); setTimeValue(initial.time); setMode("schedule"); setError(""); }

  async function scheduleMessage() {
    if (draft.kind !== "note" || !selectedConversation || isSubmitting) return;
    const parsed = parseLocalScheduledTime(dateValue, timeValue); if (!parsed.instant) { setError(parsed.error); return; }
    isSubmittingRef.current = true; setIsSubmitting(true); setError("");
    const { data, error: scheduleError } = await supabase.rpc("schedule_note_message", { target_conversation_id: selectedConversation.conversationId, content_snapshot: draft.body, scheduled_for: parsed.instant.toISOString() });
    isSubmittingRef.current = false; setIsSubmitting(false);
    if (scheduleError || !data) { setError(normalizeDeliveryError(scheduleError, true)); return; }
    const row = (Array.isArray(data) ? data[0] : data) as Record<string, unknown>;
    setSuccess({ kind: "scheduled", conversationId: selectedConversation.conversationId, scheduledFor: typeof row.scheduled_for === "string" ? row.scheduled_for : parsed.instant.toISOString() });
  }

  if (typeof document === "undefined") return null;
  return createPortal(<motion.div initial={reducedMotion ? false : { opacity: 0 }} animate={{ opacity: 1 }} exit={{ opacity: 0 }} transition={{ duration: reducedMotion ? 0 : 0.18 }} className="fixed inset-0 z-[110] flex items-end justify-center overflow-y-auto bg-heading/20 sm:items-center sm:p-5" onPointerDown={(event) => { if (event.target === event.currentTarget && !isSubmitting) onClose(); }}><motion.div ref={panelRef} initial={reducedMotion ? false : { opacity: 0, y: 14, scale: 0.99 }} animate={{ opacity: 1, y: 0, scale: 1 }} exit={reducedMotion ? { opacity: 0 } : { opacity: 0, y: 10, scale: 0.99 }} role="dialog" aria-modal="true" aria-labelledby="message-delivery-title" aria-describedby="message-delivery-description" aria-busy={isSubmitting} className="flex max-h-[min(94dvh,48rem)] w-full max-w-2xl flex-col overflow-hidden rounded-t-3xl border border-border bg-surface shadow-soft sm:rounded-3xl">{success && successConversation ? <div className="p-6 sm:p-8"><div className="flex h-12 w-12 items-center justify-center rounded-2xl bg-accent text-primary"><SendIcon /></div><h2 id="message-delivery-title" className="mt-5 text-xl font-semibold text-heading">{success.kind === "scheduled" ? "Message scheduled" : draft.kind === "forward" ? "Message forwarded" : "Note sent"}</h2><p id="message-delivery-description" role="status" aria-live="polite" className="mt-2 text-sm leading-6 text-body">{success.kind === "scheduled" ? <>Scheduled for {getConversationDisplayName(successConversation.otherProfile, successConversation.otherNickname)} on <time dateTime={success.scheduledFor} aria-label={formatScheduledInstantAccessible(success.scheduledFor)}>{formatScheduledInstant(success.scheduledFor)}</time>. Delivery may occur within approximately one minute.</> : <>Sent to {getConversationDisplayName(successConversation.otherProfile, successConversation.otherNickname)} as an independent message.</>}</p><div className="mt-7 flex flex-col-reverse gap-2 sm:flex-row sm:justify-end"><button type="button" data-success-autofocus onClick={onClose} className="min-h-11 rounded-2xl border border-border px-4 py-2.5 text-sm font-semibold text-heading hover:bg-accent focus-visible:outline-none focus-visible:ring-4 focus-visible:ring-accent-hover">Done</button>{success.kind === "sent" && <button type="button" onClick={() => onOpenConversation(successConversation)} className="min-h-11 rounded-2xl bg-primary px-4 py-2.5 text-sm font-semibold text-white hover:bg-primary-hover focus-visible:outline-none focus-visible:ring-4 focus-visible:ring-accent-hover">Open conversation</button>}</div></div> : <><header className="flex shrink-0 items-start justify-between gap-4 border-b border-border px-5 py-4 sm:px-6"><div><h2 id="message-delivery-title" className="text-lg font-semibold text-heading">{mode === "schedule" ? "Send later" : draft.kind === "forward" ? "Forward message" : "Send note"}</h2><p id="message-delivery-description" className="mt-1 text-sm text-body">{mode === "schedule" ? "Choose a local date and time for this private snapshot." : "Choose one connected conversation."}</p></div><button type="button" onClick={onClose} disabled={isSubmitting} aria-label="Close recipient picker" className="flex h-10 w-10 shrink-0 items-center justify-center rounded-xl text-muted hover:bg-accent hover:text-heading focus-visible:outline-none focus-visible:ring-2 focus-visible:ring-primary/20 disabled:opacity-50"><svg viewBox="0 0 24 24" fill="none" stroke="currentColor" strokeWidth="1.8" className="h-5 w-5" aria-hidden="true"><path d="m7 7 10 10M17 7 7 17" strokeLinecap="round" /></svg></button></header><div className="grid min-h-0 flex-1 md:grid-cols-[minmax(0,0.9fr)_minmax(0,1.1fr)]"><section className="flex min-h-0 flex-col border-b border-border md:border-b-0 md:border-r" aria-label="Recipients"><div className="shrink-0 p-4"><label htmlFor="delivery-recipient-search" className="sr-only">Search connected conversations</label><input data-autofocus id="delivery-recipient-search" type="search" value={query} onChange={(event) => setQuery(event.target.value)} placeholder="Search conversations…" disabled={isSubmitting} className="min-h-11 w-full rounded-2xl border border-border bg-background px-4 text-sm text-heading outline-none placeholder:text-muted focus:border-primary/40 focus:ring-4 focus:ring-accent-hover disabled:opacity-60" /></div><div className="min-h-40 flex-1 overflow-y-auto px-3 pb-3">{filteredConversations.length ? <ul className="space-y-1">{filteredConversations.map((conversation) => { const selected = conversation.conversationId === selectedConversationId; const name = getConversationDisplayName(conversation.otherProfile, conversation.otherNickname); return <li key={conversation.conversationId}><button type="button" onClick={() => { setSelectedConversationId(conversation.conversationId); setError(""); }} disabled={isSubmitting} aria-pressed={selected} className={`flex min-h-14 w-full items-center gap-3 rounded-2xl px-3 py-2 text-left transition focus-visible:outline-none focus-visible:ring-2 focus-visible:ring-primary/20 disabled:opacity-60 ${selected ? "bg-accent" : "hover:bg-card"}`}><ProfileAvatar profile={conversation.otherProfile} size="sm" /><span className="min-w-0 flex-1"><span className="block truncate text-sm font-semibold text-heading">{name}</span>{conversation.otherProfile.username && <span className="block truncate text-xs text-muted">@{conversation.otherProfile.username}</span>}</span><span className="text-xs font-semibold text-primary">{selected ? "Selected" : "Select"}</span></button></li>; })}</ul> : <p role="status" className="px-3 py-8 text-center text-sm leading-6 text-body">{conversations.length ? "No connected conversations match your search." : "No eligible connected conversations are available."}</p>}</div></section><section className="min-h-0 overflow-y-auto p-5 sm:p-6" aria-label={mode === "schedule" ? "Schedule and message preview" : "Message preview"}><p className="text-xs font-semibold uppercase tracking-[0.14em] text-muted">Preview</p>{draft.kind === "forward" && <p className="mt-3 text-xs font-semibold text-primary">Forwarded</p>}<div className="mt-2 max-h-52 overflow-y-auto whitespace-pre-wrap break-words rounded-2xl border border-border bg-background p-4 text-sm leading-6 text-body">{draft.preview}</div>{draft.kind === "note" && draft.wasTruncated && <p className="mt-3 text-xs leading-5 text-body">The message uses the first 2,000 characters of this note snapshot.</p>}{draft.kind === "note" && draft.omittedAttachmentCount > 0 && <p className="mt-3 text-xs leading-5 text-body">Attachments aren't included in {mode === "schedule" ? "scheduled messages" : "messages sent from Notes"} yet.</p>}{mode === "schedule" && <div className="mt-5"><p className="text-sm font-semibold text-heading">Send to {selectedConversation ? getConversationDisplayName(selectedConversation.otherProfile, selectedConversation.otherNickname) : "a recipient"}</p><div className="mt-4 grid gap-4 sm:grid-cols-2"><label htmlFor="scheduled-message-date" className="text-sm font-semibold text-heading">Date<input id="scheduled-message-date" type="date" value={dateValue} onChange={(event) => { setDateValue(event.target.value); setError(""); }} disabled={isSubmitting} className="mt-2 min-h-12 w-full rounded-2xl border border-border bg-background px-4 text-heading outline-none focus:border-primary/40 focus:ring-4 focus:ring-accent-hover" /></label><label htmlFor="scheduled-message-time" className="text-sm font-semibold text-heading">Time<input id="scheduled-message-time" type="time" value={timeValue} onChange={(event) => { setTimeValue(event.target.value); setError(""); }} disabled={isSubmitting} className="mt-2 min-h-12 w-full rounded-2xl border border-border bg-background px-4 text-heading outline-none focus:border-primary/40 focus:ring-4 focus:ring-accent-hover" /></label></div>{parsedSchedule.instant && <p role="status" className="mt-3 text-xs leading-5 text-body">Local time: <time dateTime={parsedSchedule.instant.toISOString()} aria-label={formatScheduledInstantAccessible(parsedSchedule.instant.toISOString())}>{formatScheduledInstant(parsedSchedule.instant.toISOString())}</time></p>}</div>}{error && <p role="alert" className="mt-4 rounded-2xl border border-primary/25 bg-accent px-4 py-3 text-sm leading-6 text-body">{error}</p>}</section></div><footer className="flex shrink-0 flex-col-reverse gap-2 border-t border-border px-5 py-4 sm:flex-row sm:items-center sm:justify-end sm:px-6"><button type="button" onClick={mode === "schedule" ? () => { setMode("choose"); setError(""); } : onClose} disabled={isSubmitting} className="min-h-11 rounded-2xl border border-border px-4 py-2.5 text-sm font-semibold text-heading hover:bg-accent focus-visible:outline-none focus-visible:ring-4 focus-visible:ring-accent-hover disabled:opacity-50">{mode === "schedule" ? "Back" : "Cancel"}</button>{mode === "choose" && draft.kind === "note" && <button type="button" onClick={beginScheduling} disabled={!selectedConversation || isSubmitting} className="min-h-11 rounded-2xl border border-border px-4 py-2.5 text-sm font-semibold text-heading hover:bg-accent focus-visible:outline-none focus-visible:ring-4 focus-visible:ring-accent-hover disabled:opacity-50">Send later</button>}{mode === "choose" ? <button type="button" onClick={() => void sendNow()} disabled={!selectedConversation || isSubmitting} className="inline-flex min-h-11 items-center justify-center gap-2 rounded-2xl bg-primary px-5 py-2.5 text-sm font-semibold text-white hover:bg-primary-hover focus-visible:outline-none focus-visible:ring-4 focus-visible:ring-accent-hover disabled:cursor-not-allowed disabled:opacity-50"><SendIcon />{isSubmitting ? "Sending…" : draft.kind === "forward" ? "Forward" : "Send now"}</button> : <button type="button" onClick={() => void scheduleMessage()} disabled={!selectedConversation || !dateValue || !timeValue || Boolean(parsedSchedule.error) || isSubmitting} className="inline-flex min-h-11 items-center justify-center gap-2 rounded-2xl bg-primary px-5 py-2.5 text-sm font-semibold text-white hover:bg-primary-hover focus-visible:outline-none focus-visible:ring-4 focus-visible:ring-accent-hover disabled:cursor-not-allowed disabled:opacity-50"><SendIcon />{isSubmitting ? "Scheduling…" : "Schedule message"}</button>}</footer></>}</motion.div></motion.div>, document.body);
}

export default MessageDeliveryDialog;

export type DashboardSection = "messages" | "archived" | "people" | "requests" | "menu";

export type WorkspaceLayoutMode = "workspace" | "focus";

export type WorkspaceLayoutState = {
  mode: WorkspaceLayoutMode;
};
